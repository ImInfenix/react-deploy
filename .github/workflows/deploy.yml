name: Deploy 🚀

on:
  push:
    branches: [main]

jobs:
  build:
    name: Deploy 🚀
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        name: Setup Node.js

      - run: npm ci

      - run: npm run deploy
        name: Deploy application
